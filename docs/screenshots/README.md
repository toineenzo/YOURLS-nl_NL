# Screenshots

Drop image files into this folder so the README renders them. Expected:

| Filename | Shows |
| --- | --- |
| `admin-nl.png` | The YOURLS admin interface running in Dutch (top of the README). |

Recommended dimensions: 1440×~ (will be displayed at 720 px wide).
